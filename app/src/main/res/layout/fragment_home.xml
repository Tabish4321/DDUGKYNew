<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorSurface">

    <!-- Main Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include layout="@layout/layout_toolbar_base" />

        <FrameLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvModules"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:overScrollMode="never"
                android:scrollbars="none"
                android:paddingHorizontal="@dimen/dp_12"
                android:paddingTop="@dimen/dp_12"
                android:paddingBottom="@dimen/dp_80"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:listitem="@layout/item_module" />
        </FrameLayout>
    </LinearLayout>

    <!-- Fullscreen Progress -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        app:indicatorColor="@color/login_btn"
        app:trackColor="@color/color_border_subtle"
        app:indicatorSize="56dp"
        app:trackThickness="5dp"
        android:visibility="gone" />

    <!-- Navigation Drawer - Modern Look -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/bg_nav_drawer_rounded"
        android:elevation="16dp"
        android:fitsSystemWindows="true"
        app:itemHorizontalPadding="20dp"
        app:itemVerticalPadding="14dp"
        app:itemIconPadding="16dp"
        app:itemIconSize="22dp"
        app:itemShapeAppearanceOverlay="@style/NavItemShape"
        app:itemTextColor="@color/login_btn"
        app:itemIconTint="@color/button_solid_light"
        app:itemBackground="@drawable/nav_item_background"
        app:itemRippleColor="@color/login_btn"
        app:itemTextAppearance="@style/NavTextModern"
        app:menu="@menu/menu_main"
        app:headerLayout="@layout/navigation_header" />

</androidx.drawerlayout.widget.DrawerLayout>